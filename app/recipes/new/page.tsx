// TODO: Implement new recipe page
// - Render <RecipeForm> in "create" mode
// - On submit, POST to /api/recipes
// - Redirect to /recipes/[id] on success
// - Protected route: redirect to /auth/login if not authenticated

export default function NewRecipePage() {
  return null;
}
