// TODO: Implement recipes list page
// - Fetch the current user's recipes from Supabase
// - Display them using <RecipeCard> components
// - Include a link/button to create a new recipe (/recipes/new)
// - Protected route: redirect to /auth/login if not authenticated

export default function RecipesPage() {
  return null;
}
