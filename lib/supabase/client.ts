// TODO: Implement Supabase browser client
// - Create and export a Supabase client for use in Client Components
// - Uses @supabase/ssr createBrowserClient
// - Reads NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY from env

import { createBrowserClient } from "@supabase/ssr";

// TODO: export createClient function
export function createClient() {
  // TODO: return createBrowserClient(url, anonKey)
}
